{
  "name": "back_core",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run index.ts",
    "start": "bun run index.ts",
    "build": "bun run --bun tsc --noEmit && echo 'Build completed successfully'",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:client": "bun run tests/client_index.ts",
    "example:basic": "bun run examples/basic-usage.ts",
    "example:server": "bun run examples/express-server.ts",
    "example:client": "bun run examples/client-example.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "supertest": "^7.0.0",
    "@types/supertest": "^6.0.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@cloudflare/voprf-ts": "^1.0.0",
    "@types/express": "^5.0.3",
    "@types/swagger-jsdoc": "^6.0.4",
    "@types/swagger-ui-express": "^4.1.8",
    "express": "^5.1.0",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  }
}
